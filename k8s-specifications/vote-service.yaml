apiVersion: v1
kind: Service
metadata:
  name: vote
  namespace: voting-app
  labels:
    app: vote
spec:
  type: LoadBalancer   # ✅ external access
  selector:
    app: vote
  ports:
  - name: http
    port: 8080
    targetPort: 80